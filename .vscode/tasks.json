{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Fortran",
            "type": "shell",
            "command": "gfortran -fcheck=all ${fileBasename} -o ${fileBasenameNoExtension}.out && ./${fileBasenameNoExtension}.out",
            "args": [],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}